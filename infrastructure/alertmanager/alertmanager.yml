route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'cirdi-webhook'

receivers:
- name: 'cirdi-webhook'
  webhook_configs:
  - url: 'http://cirdi-app:3000/api/alerts/webhook'
    send_resolved: true
